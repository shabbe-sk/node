{
  "name": "job-portal-platform",
  "version": "1.0.0",
  "description": "Enterprise Job Portal Platform - MERN Stack",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "install-all": "npm install && cd frontend && npm install",
    "dev-all": "concurrently \"npm run dev\" \"cd frontend && npm start\"",
    "build": "cd frontend && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["job", "portal", "mern", "recruitment", "ats"],
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.0.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.0",
    "dotenv": "^16.0.3",
    "cors": "^2.8.5",
    "express-validator": "^7.0.0",
    "multer": "^1.4.5-lts.1",
    "socket.io": "^4.5.4",
    "node-cron": "^3.0.2",
    "nodemailer": "^6.9.1",
    "stripe": "^12.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "concurrently": "^8.0.0"
  }
}
